<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head', { title: 'otp for Sign up' }) %>
  <body class=" d-flex justify-content-center align-items-center vh-100" >

    <div class="container p-4 shadow rounded-4 overflow-hidden " style="max-width: 900px;background-color: rgb(244, 236, 227) !important;">
      <div class="row g-0">

        <!-- left Section (Image) -->
        <div class="col-md-6 d-none d-md-block">
          <img src="/images/otp-signup.png" alt="Login Visual" class="img-fluid h-100 w-100 object-fit-cover" style="object-fit: cover;" />
        </div>

        <!-- Right Section OTP -->
        <div class="col-md-6  p-5 d-flex flex-column justify-content-center">
          <h2><b>Confirm with OTP</b></h2>

          <form action="/verify-otp" method="POST">
            <div class="d-flex justify-content-center gap-2 mb-3">
            <input type="text" name="otp[]" maxlength="1" class="form-control text-center otp-input" required>
            <input type="text" name="otp[]" maxlength="1" class="form-control text-center otp-input" required>
            <input type="text" name="otp[]" maxlength="1" class="form-control text-center otp-input" required>
            <input type="text" name="otp[]" maxlength="1" class="form-control text-center otp-input" required>
            </div>

            <button type="submit" class="btn btn-dark w-100">Verify OTP</button>
         </form>
            <p class="mt-3 text-muted" id="resend-container">Resend OTP in <span class="text-start text-muted" id="timer">60</span>seconds.
            </p>

            <a href="/signup/resend-otp" id="resend-link" class="d-none text-primary text-decoration-none">Resend OTP</a>
            
          <p class="mt-4 text-center text-muted">
          <a href="/signup" class="text-primary text-decoration-none">Go back to sign up page</a>
          </p>
        </div>

        
      </div>
    </div>

    <!-- Include your external script -->
    <script src="/js/otp-timer.js"></script>
  </body>
</html>
